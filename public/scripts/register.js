"use strict";

$(function()
{
    // url parameters stored in a variable
    let urlParams = new urlParams(location.search)
    // teamid is parsed from the url and stored in a variable
    let teamid = urlParams.get("courseid");

    




})

function getTeamForm()
{

}